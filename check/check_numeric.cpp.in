#include <type_traits>

using Real = $CACHE{Sidis_REAL_TYPE};

static_assert(
	std::is_floating_point<Real>::value,
	"sidis::Real must be a floating point type");

int main(int argc, char** argv) {
	return 0;
}

